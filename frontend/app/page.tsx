import React from 'react';
import LoginPage from '@/app/login/page'; // Adjust the path to where your LoginPage component is

const App = () => {
  return (
    <div>
      <LoginPage />
    </div>
  );
};

export default App;
